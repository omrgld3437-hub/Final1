[pytest]
pythonpath = .
addopts = -m "not slow"
markers =
    asyncio: run async test functions with the local asyncio hook
    integration: tests that need optional live/local data
    slow: full 200k pool build or heavy performance tests (set DPS_FULL_POOL=1)
